<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.ac.knue.cms.systemconfig.SystemConfigMapper">
  <sql id="columns">
    config_id AS "configId",
    setting_key AS "settingKey",
    setting_name AS "settingName",
    setting_value AS "settingValue",
    value_type AS "valueType",
    unit,
    default_value AS "defaultValue",
    min_value AS "minValue",
    max_value AS "maxValue",
    description,
    use_yn AS "useYn",
    created_at AS "createdAt",
    updated_at AS "updatedAt",
    updated_by AS "updatedBy"
  </sql>
  <select id="listActive" resultType="map">
    SELECT <include refid="columns" />
    FROM cmn_system_config
    WHERE use_yn = 'Y'
    ORDER BY config_id
  </select>
  <select id="findByKey" resultType="map">
    SELECT <include refid="columns" />
    FROM cmn_system_config
    WHERE setting_key = #{settingKey}
  </select>
  <update id="updateValue">
    UPDATE cmn_system_config
    SET setting_value = #{settingValue}, updated_by = #{updatedBy}, updated_at = now()
    WHERE setting_key = #{settingKey} AND use_yn = 'Y'
  </update>
</mapper>
