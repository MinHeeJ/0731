<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.ac.knue.performance.menu.MenuMapper">
  <select id="list" resultType="kr.ac.knue.performance.menu.MenuDto">select menu_id as "menuId", parent_menu_id as "parentMenuId", menu_name as "menuName", menu_level as "menuLevel", display_order as "displayOrder", screen_id as "screenId", url_path as "urlPath", icon_name as "iconName", business_category as "businessCategory", description, is_active as "isActive" from menu <where><if test="parentMenuId != null">and parent_menu_id=#{parentMenuId}</if><if test="screenId != null">and screen_id=#{screenId}</if><if test="menuName != null">and menu_name like concat('%', #{menuName}, '%')</if></where> order by parent_menu_id nulls first, display_order</select>
  <insert id="insert">insert into menu(parent_menu_id, menu_name, menu_level, display_order, screen_id, url_path, icon_name, business_category, description, is_active) values (#{request.parentMenuId}, #{request.menuName}, #{request.menuLevel}, #{request.displayOrder}, #{request.screenId}, #{request.urlPath}, #{request.iconName}, #{request.businessCategory}, #{request.description}, coalesce(#{request.isActive}, true))</insert>
  <update id="update">update menu set parent_menu_id=#{request.parentMenuId}, menu_name=#{request.menuName}, menu_level=#{request.menuLevel}, display_order=#{request.displayOrder}, screen_id=#{request.screenId}, url_path=#{request.urlPath}, icon_name=#{request.iconName}, business_category=#{request.businessCategory}, description=#{request.description}, is_active=coalesce(#{request.isActive}, true), updated_at=now() where menu_id=#{menuId}</update>
  <select id="findLatestByName" resultType="kr.ac.knue.performance.menu.MenuDto">select menu_id as "menuId", parent_menu_id as "parentMenuId", menu_name as "menuName", menu_level as "menuLevel", display_order as "displayOrder", screen_id as "screenId", url_path as "urlPath", icon_name as "iconName", business_category as "businessCategory", description, is_active as "isActive" from menu where menu_name=#{menuName} order by created_at desc limit 1</select>
  <select id="find" resultType="kr.ac.knue.performance.menu.MenuDto">select menu_id as "menuId", parent_menu_id as "parentMenuId", menu_name as "menuName", menu_level as "menuLevel", display_order as "displayOrder", screen_id as "screenId", url_path as "urlPath", icon_name as "iconName", business_category as "businessCategory", description, is_active as "isActive" from menu where menu_id=#{menuId}</select>
  <select id="listPermissions" resultType="kr.ac.knue.performance.menu.MenuPermissionDto">select permission_id as "permissionId", target_type as "targetType", target_id as "targetId", menu_id as "menuId", allowed from menu_permission <where><if test="targetType != null">and target_type=#{targetType}</if><if test="targetId != null">and target_id=#{targetId}</if></where> order by target_type, target_id</select>
  <update id="updatePermission">update menu_permission set allowed=#{allowed}, updated_at=now() where target_type=#{targetType} and target_id=#{targetId} and menu_id=#{menuId}</update>
  <insert id="insertPermission">insert into menu_permission(target_type, target_id, menu_id, allowed) values (#{targetType}, #{targetId}, #{menuId}, #{allowed})</insert>
</mapper>
