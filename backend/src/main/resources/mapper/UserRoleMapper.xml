<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.ac.knue.performance.userrole.UserRoleMapper">
  <select id="list" resultType="kr.ac.knue.performance.userrole.UserRoleAssignmentDto">select assignment_id as "assignmentId", user_id as "userId", role_code as "roleCode", assignment_type as "assignmentType", valid_from as "validFrom", valid_to as "validTo", approved_by_user_id as "approvedByUserId", status from user_role_assignment <where><if test="userId != null">and user_id=#{userId}</if><if test="roleCode != null">and role_code=#{roleCode}</if><if test="status != null">and status=#{status}</if></where> order by valid_from desc</select>
  <insert id="insert">insert into user_role_assignment(user_id, role_code, assignment_type, valid_from, valid_to, approved_by_user_id, status) values (#{request.userId}, #{request.roleCode}, coalesce(#{request.assignmentType}, 'MANUAL'), #{request.validFrom}, #{request.validTo}, #{actor}, 'ACTIVE')</insert>
  <update id="revoke">update user_role_assignment set status='REVOKED', approved_by_user_id=#{actor}, processed_at=now(), updated_at=now() where assignment_id=#{assignmentId} and status='ACTIVE'</update>
  <select id="find" resultType="kr.ac.knue.performance.userrole.UserRoleAssignmentDto">select assignment_id as "assignmentId", user_id as "userId", role_code as "roleCode", assignment_type as "assignmentType", valid_from as "validFrom", valid_to as "validTo", approved_by_user_id as "approvedByUserId", status from user_role_assignment where assignment_id=#{assignmentId}</select>
  <select id="countRole" resultType="int">select count(*) from role where role_code=#{roleCode} and is_active=true</select>
</mapper>
